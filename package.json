{
  "name": "stencil-oms",
  "version": "1.0.0",
  "private": true,
  "description": "Stencil Order Management System — Express + MongoDB backend serving a standalone frontend",
  "scripts": {
    "setup": "npm --prefix Backend install",
    "seed": "npm --prefix Backend run seed",
    "dev": "npm --prefix Backend run dev",
    "start": "npm --prefix Backend start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
